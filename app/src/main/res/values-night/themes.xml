<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- The demo's Compose UI already follows the system's dark mode, and the card form follows
         the host theme's colors, so the host theme needs a night variant for the two to agree. -->
    <style name="Theme.ExampleTheme" parent="android:Theme.Material.NoActionBar" />
</resources>
