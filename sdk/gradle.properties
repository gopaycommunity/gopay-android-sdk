# GoPay SDK Publishing Properties
sdk.version=1.0.0
sdk.groupId=cz.gopay
sdk.artifactId=sdk

# Publishing credentials (set these in your local gradle.properties or environment)
# mavenUsername=your-username
# mavenPassword=your-password

# Repository URLs (uncomment and configure as needed)
# mavenReleaseUrl=https://your-maven-repo.com/releases
# mavenSnapshotUrl=https://your-maven-repo.com/snapshots 
